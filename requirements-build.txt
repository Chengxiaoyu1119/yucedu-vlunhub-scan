# The Windows build stages Chromium and the Node driver beside the EXE.
# The Playwright protocol runtime stays in the EXE so the package remains
# portable while screenshots keep working without a separate Python install.
pywebview>=5.0,<6
playwright>=1.40,<2
pyinstaller>=6.0,<7
